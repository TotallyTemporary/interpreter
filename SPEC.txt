
# Indentation based blocks.


int fib(int times) is
    if times == 0 then return 0
    if times == 1 then return 1
    return fib(times - 1) + fib(times - 2)


if something == 1 then
    return 1 # awoo!


int something = 2
if something + 2 == 4 then
    return something


str some_string = "Hello" + " world!"
print(some_string)

some_string[0] = 'h'
print(some_string)

class Vector3i is
    int x
    int y
    int z

    void new(int x, int y, int z) is
        this.x = x
        this.y = y
        this.z = z

    Vector3i add(Vector3i other) is
        return new Vector3i(this.x + other.x, this.y + other.y, this.z + other.z)


void main() is
    Vector3i pos = new Vector3i(1, 0, 0)
    Vector3i anew = pos.add(new Vector3i(0, 1, 1))
    print(pos.x)
    print(pos.y)
    print(pos.z)
    print(anew.x)
    print(anew.y)
    print(anew.z)






class Printer is
    Printer new() is nop
    void print_something() is print(2)
    
void main() is
    Printer printer = new Printer()
    printer.print_something()